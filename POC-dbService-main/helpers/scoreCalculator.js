const scoreCalc = (questionnaireId, answers) => {
    answers.forEach(answer => {
        //TODO: change to calculation of score (use switch-case for each questionnaire).
        // e.g.: if questionnaireId == "q_001": calcQ1(answers)
    })
    const scoreObj = {name: "placeholder", value: "100"}
    return(scoreObj)
}

module.exports = scoreCalc